@model List<MultivendorEcommerceStore.DB.ViewModel.FeatureProductsViewModel>

<!-- featured category fashion -->
<div class="category-featured fashion">
    <nav class="navbar nav-menu show-brand">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-brand"><a href="#"><img alt="fashion" src="~/assets/data/icon-fashion.png" />fashion</a></div>
            <span class="toggle-menu"></span>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="active"><a data-toggle="tab" href="#tab-4">Best Sallers</a></li>
                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
        <div id="elevator-1" class="floor-elevator">
            <a href="#" class="btn-elevator up disabled fa fa-angle-up"></a>
            <a href="#elevator-2" class="btn-elevator down fa fa-angle-down"></a>
        </div>
    </nav>
    <div class="product-featured clearfix">
        <div class="row">
            <div class="col-sm-2 sub-category-wapper">
                <ul class="sub-category-list">
                    @foreach (var Mainitem in Model)
                    {
                        foreach (var item in Mainitem.CategoryListVM.Where(c => c.DisplayOrder == 2 & c.CategoryName == "FASHION").ToList())
                        {
                            foreach (var SubCat in item.listSubCategoryEntity.OrderBy(c => c.SubCategoryName))
                            {
                                <li>
                                    <a href="@Url.Action("Products", "Home", new{ PId = SubCat.SubCategoryID })">
                                        @Html.DisplayFor(modelitem => SubCat.SubCategoryName)
                                    </a>
                                </li>
                            }
                        }
                    }
                </ul>
            </div>

            <div class="col-sm-10 col-right-tab">
                <div class="product-featured-tab-content">
                    <div class="tab-container">
                        <div class="tab-panel active" id="tab-4">
                            <div class="box-left">
                                @foreach (var Mianitem in Model)
                                {
                                    foreach (var item in Mianitem.CategoryListVM.Where(c => c.DisplayOrder == 2 & c.CategoryName == "FASHION").ToList())
                                    {
                                        <div class="banner-img">
                                            <a href="#"><img src="@Html.DisplayFor(modelItem => item.CategoryPicture)"></a>
                                        </div>

                                    }
                                }
                            </div>

                            <div class="box-right">
                                <ul class="product-list row">
                                    @foreach (var featureProduct in Model)
                                    {
                                        foreach (var item in featureProduct.ProductListVM.Where(p => p.Active == "Yes" && p.FeatureProduct == true && p.CategoryName == "FASHION"))
                                        {
                                            <li class="col-sm-4">
                                                <div class="right-block">
                                                    <h5 class="product-name"><a href="#">@item.ProductName</a></h5>
                                                    <div class="content_price">
                                                        <span class="price product-price">Rs. @item.Price</span>
                                                    </div>
                                                </div>
                                                <div class="left-block">
                                                    <a href="@Url.Action("ProductDetail", "Home", new { ProductID = item.ProductID })"><img class="img-responsive" alt="product" src="@Url.Content(item.ProductImage1)" width="268" height="327" /></a>
                                                    <div class="quick-view">
                                                        <a title="Add to my wishlist" class="heart" href="#"></a>
                                                        <a title="Add to compare" class="compare" href="#"></a>
                                                        <a title="Quick view" class="search" href="#"></a>
                                                    </div>
                                                    <div class="add-to-cart">
                                                        <button title="Add to Cart" value="@item.ProductID" class="AddToCart" type="button" style="background-color: rgba(255, 51, 102, 0.7);
    background: rgba(255, 51, 102, 0.7);
    color: rgba(255, 51, 102, 0.7);">Add to Cart</button>
                                                        @*<a title="Add to Cart" href="#">Add to Cart</a>*@
                                                    </div>
                                                </div>
                                            </li>
                                        }
                                    }

                                </ul>
                            </div>

                        </div>




                    </div>
                </div>
            </div>
        </div>



    </div>
</div>
<!-- end featured category fashion -->
